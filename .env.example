# Hostinger MySQL Database Credentials
# Go to Settings -> Secrets in GitHub and add these values (DO NOT commit secrets):
# DB_HOST, DB_USER, DB_PASSWORD, DB_NAME, DB_PORT
# For DB_HOST: use your Hostinger MySQL host (e.g. 82.112.228.35)
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=3306

# Google Sheet -> NPD sync
NPD_SYNC_SECRET=
NPD_SYNC_ALLOWED_TAB=NPD
